# pnpm install từ chối chạy nếu Node không khớp `engines.node` trong package.json.
# Lý do: better-sqlite3 là module native, đổi major Node mà không rebuild thì
# mọi query DB hỏng im lặng — triệu chứng là "sai mật khẩu" (16/09/2026).
engine-strict=true
